export enum TRANSACTION_SOURCE {
    CASH = 0,
    MOMO = 1,
    BANK = 2
}

export enum REPORT_BY {
    WEEK = 0,
    MONTH = 1,
    YEAR = 2
}

export enum CALENDAR_STYLE {
    THEME = 0,
    TEXT = 1,
    CURRENT_DATE = 2
}